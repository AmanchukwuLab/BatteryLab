# Overview
This was first encountered after a session of reteaching well positions for the assembly robot: the lookup camera had timed out. This left it in some stalled state in which it couldn't be used. The simplest fix was to ==unplug and reconnect the camera (attached to rasp5)==. 

