# Overview
After taking a picture of a component using the lookup camera, the image does not appear in Research/images as expected. 

# Fix
1. Check all ```image_path``` vars in AssemblyRobot.py and ensure they point to the correct file. Check for extra forwards slashes. 
